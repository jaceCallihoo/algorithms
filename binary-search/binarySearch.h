#include <vector>

using namespace std;

class BinarySearch {
public:
	int find(vector<int> array, int target);
	int findFirst(vector<int> array, int target);
	int findLast(vector<int> array, int target);
};
